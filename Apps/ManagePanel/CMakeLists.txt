if (NOT ROD_BUILD_MANAGE_PANEL)
    message(STATUS "Manage panel build disabled")
    return()
endif()

COMPONENTS_CCR_ADD_QT(5 Core Widgets Network)

# TODO: How to move it into CMake component????
# Qt configuration
set(CMAKE_AUTOUIC ON)
set(CMAKE_AUTOMOC ON)
set(CMAKE_AUTORCC ON)

set(MPANEL_TARGET_NAME ROD-ManagePanel)

COMPONENTS_CCR_CONFIGURE_APP(${MPANEL_TARGET_NAME} ${CMAKE_CURRENT_LIST_DIR}
    Qt5::Widgets
    Qt5::Network
)

COMPONENTS_LINK_COMPONENT(${MPANEL_TARGET_NAME} Logger)
COMPONENTS_LINK_COMPONENT(${MPANEL_TARGET_NAME} Common)

set(ROD_LIBRARIES_ROOT ${CMAKE_CURRENT_SOURCE_DIR}/../../Libraries)
COMPONENTS_CCR_LINK_LIBRARY(${MPANEL_TARGET_NAME} DataObjects ${ROD_LIBRARIES_ROOT}/DataObjects)
COMPONENTS_CCR_LINK_LIBRARY(${MPANEL_TARGET_NAME} Protocol ${ROD_LIBRARIES_ROOT}/Protocol)
